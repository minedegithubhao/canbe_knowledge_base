alias:: Normalization
tags::
type:: 概念
status:: 草稿

- Normalization，常用 Min-Max 归一化
- 计算公式
	- $$
	  x' = \frac{x - x_{\min}}{x_{\max} - x_{\min}}
	  $$
	- `x' = (当前值 - 该列最小值) / (该列最大值 - 该列最小值)`
- API调用
	- ```python
	  # 创建归一化对象
	  transfer = MinMaxScaler()
	  # 对原数据集进行归一化操作
	  x_train_new = transfer.fit_transform(x_train)
	  ```