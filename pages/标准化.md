alias:: Standardization
type:: concept
status:: archived

- **迁移说明**
	- 本页面内容已整理到 [[Concept/特征缩放]] 和 [[Question/归一化和标准化有什么区别]]。
	- 后续请维护新页面；本页仅保留旧笔记和反向链接。

- 计算公式
	- $$
	  x' = \frac{x - \mu}{\sigma}
	  $$
	- `x'  = (当前值 - 该列平均值) / 该列的标准差`
	- `标准差计算公式: 方差开平方根`
	- `方差 = 每个值 和 均值的差 的平方和 的 平均值.`
- API调用
	- ```python
	  # 2. 创建标准化对象.
	  transfer = StandardScaler()
	  # 3. 对原数据集进行标准化操作.
	  x_train_new = transfer.fit_transform(x_train)
	  ```
